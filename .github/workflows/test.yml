on:
- push
- pull_request
name: test
jobs:
  test:
    strategy:
      matrix:
        go-version:
        - 1.13.x
        - 1.14.x
        os:
        - macos-latest
        - ubuntu-latest
        - windows-latest
    runs-on: ${{ matrix.os }}
    steps:
    - name: Setup Go
      uses: actions/setup-go@v1
      with:
        go-version: ${{ matrix.go-version }}
    - name: Checkout
      uses: actions/checkout@v1
    - name: Go test
      run: go test ./...
